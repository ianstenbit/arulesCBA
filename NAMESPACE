useDynLib(arulesCBA)

import("discretization")
import("arules")
import("Matrix")
import("testthat")
import("glmnet")

importFrom("methods", "as", "is", "new")
importFrom("stats", "as.formula", "df")

export("CBA",
       "RCAR",
       "rules",
       "CBA_ruleset",
       "bCBA",
       "wCBA",
       "discretizeDF.supervised",
       "mineCARs",
       "classFrequency"
       )

S3method(predict, CBA)
S3method(print, CBA)
S3method(rules, CBA)


S3method(predict, RCAR)
S3method(print, RCAR)